import { incrementCounter } from './incrementCounter';
import { incrementCounterAndRegisterMetric } from './incrementCounterAndRegisterMetric';
import { registerMetric } from './registerMetric';

export { incrementCounter, incrementCounterAndRegisterMetric, registerMetric };
